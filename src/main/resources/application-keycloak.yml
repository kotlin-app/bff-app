spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:8180/realms/kotori-market}
          jwk-set-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:8180/realms/kotori-market}/protocol/openid-connect/certs
